# 引き継ぎ資料：PC での定期実行（オッズ取得・予想・保存）（競艇 v24 → 競輪アプリ）

2026-09-25 作成。`keirin_odds_handover.md`（オッズの使い方）の続編。
こちらは **「それを毎日、締切に間に合うように PC で回す仕組み」** の話。

v24 は 2026-09-20 に GitHub Actions から Windows PC（タスクスケジューラ）へ移した。
その後の1週間で実機で踏んだ地雷も含めて書いてある。v24 の詳しい手順は `pc/README.md`。

---

## 1. 全体構成

```
Windows PC（C:\boat）── タスクスケジューラ ─────────────────────────────
  06:00           motor     前日の結果の取り込み・精算・直前情報とオッズ記録の保存
  07/10/13:00     prefetch  当日の出走表を先に取って cache/ へ
  07:57〜23:57    yosou     3分おきに1回ずつ：締切15〜2分前のレースだけ
                              オッズ取得 → 予想 → 買い目 → 通知(ntfy) → 記録
       │
       │ 毎回 runner.py が「取り込み → 実行 → commit → push」
       ▼
GitHub（honda1986/v24）
  history.json … 買い目・結果（予想サイトと自動投票が読む）
  Pages       … 予想サイト（index.html が history.json を読む）
  Actions     … watchdog（毎時：PC が止まっていないか）と daily（23:50 集計）だけ
                yosou 等は手動実行の「予備運転」としてだけ残す

同じ PC の auto_bet（自動投票）が手元の history.json を2分おきに読んで投票
```

---

## 2. なぜ GitHub Actions から PC に移したか

| Actions で困ったこと | PC で解決したこと |
|---|---|
| `schedule` の起動が遅れる・落ちる（3回まわしても落ちる回があった） | タスクスケジューラの起動は正確。3分おきに確実に動く |
| そのため「3時間走り続けるジョブの中でループ」という作りにしていた。途中で落ちると全部失う | **1回ごとに終わる**作りにできた。落ちても失うのは3分ぶん |
| 取得元のサイトに Actions から時々まったく届かない | 家の回線から取れる。念のため出走表は朝に先取り（§7-3） |
| 自動投票が GitHub の raw ファイルを読むと5分キャッシュ＋push 待ちで遅れる | 同じ PC の history.json を直接読む。締切ぎりぎりでも間に合う |

**競輪で最初から PC で組むなら、§3〜§6 をそのまま真似すればよい。**

---

## 3. PC の準備

### 3-1. フォルダと Python

```
C:\boat\
  venv\     仮想環境（Python 3.11）
  v24\      リポジトリ（作業フォルダ）
  v22\      過去データ（読むだけ。sparse-checkout で必要なフォルダだけ）
  logs\     ログ（タスク_日付.log）
```

- `pc\setup.bat` をダブルクリックすると、venv・ライブラリ・ログ置き場・通知先の設定から、
  テストと試し運転（通知しない・push しない）までやる。何度やり直しても壊れない。
- LightGBM のモデルは文字形式（`version=v4`）なので、4系ならどの版で読んでも同じ値。
  **setup でモデルを実際に読んで木の数を出す**ところまで確かめる。

### 3-2. Windows 特有の必須設定

| 設定 | やらないと起きること |
|---|---|
| `git config --global core.autocrlf false`（**clone の前に**） | 改行が CRLF になり、JSON が毎回「全行変更」になって統合が壊れる |
| `PYTHONUTF8=1`（`task.bat` で設定） | Windows の Python は既定で cp932。日本語の JSON が化ける |
| スリープなし／ノートは「閉じても何もしない」 | 夜中に止まる。タスクには「スリープを解除して実行」も付ける |
| Windows Update のアクティブ時間を 6:00〜24:00 | 勝手な再起動が最大の停止原因 |
| タイムゾーン UTC+9・時刻の自動設定オン | 締切まで何分かの計算がずれる |
| 通知先 `setx NTFY_TOPIC "…"`（ユーザー環境変数） | 通知が飛ばない（トークンや秘密をファイルに書かない） |

### 3-3. GitHub への push（★ここで一度固まった）

- fine-grained PAT（対象リポジトリだけ・Contents の読み書き・期限1年）を作り、
  **画面のある窓で1回 push して** Git Credential Manager に覚えさせる。
- ★タスクスケジューラには画面が無い。そこで初めて認証を求められると
  **入力窓を出せずに固まり**、次の周からはロックのせいで全部「まだ動いています」で飛ばされた
  （2026-09-20 実機）。
- runner.py は git に人を待たせない環境変数を必ず渡す:

```python
GIT_ENV = {"GIT_TERMINAL_PROMPT": "0", "GCM_INTERACTIVE": "never",
           "GIT_ASKPASS": "", "SSH_ASKPASS": ""}
GIT_TIMEOUT = 180     # どんなに遅くてもここで戻る
```

  認証が無ければ黙って「push できず（次周に持ち越し）」になるだけで済む。

---

## 4. タスクスケジューラ

### 4-1. 登録するタスク（v24）

| タスク | 起動 | 打ち切り | 備考 |
|---|---|---|---|
| `boat_motor` | 毎日 6:00 | 60分 | 失敗したら15分後に3回まで再試行 |
| `boat_prefetch` | 7:00 / 10:00 / 13:00 | 30分 | 出走表の先取り |
| `boat_yosou` | 7:57 から**3分おきに16時間** | 10分 | 本体 |

共通の設定（`pc\register_tasks.ps1`）:

- ログオンしていなくても実行（`-LogonType S4U`）
- スリープを解除して実行（`-WakeToRun`）、バッテリーでも実行
- **既に実行中なら新しく始めない**（`-MultipleInstances IgnoreNew`）
- 予定時刻に動けなかったら後で動く（`-StartWhenAvailable`）
- 作業フォルダを必ずリポジトリにする（既定の `C:\Windows\System32` だとファイルが見つからない）

`pc\tasks.bat` を**右クリック →「管理者として実行」**で登録する。

### 4-2. ★登録スクリプトの地雷（2日間気づかなかった）

1. `RestartInterval` は **`"PT15M"`（ISO 8601）** で入れる。`New-TimeSpan` を後から代入すると
   XML に `00:15:00` と書かれ、`Register-ScheduledTask` が
   「タスク XML に…Interval:00:15:00」で落ちる。
2. `Register-ScheduledTask` のエラーは**スクリプトを止めない種類**。motor だけ赤字で失敗し、
   残り2つは登録されて最後まで走った。→ **`-ErrorAction Stop` を付ける。**
3. 人が確認コマンドを打つのを当てにしない。**スクリプトの最後で自分で数えて、足りなければ止める。**

```powershell
$want = @("boat_motor", "boat_prefetch", "boat_yosou")
$have = @(Get-ScheduledTask -TaskName $want -ErrorAction SilentlyContinue |
          Select-Object -ExpandProperty TaskName)
$miss = @($want | Where-Object { $_ -notin $have })
if ($miss.Count -gt 0) { throw "タスクが $($miss.Count) 個足りません" }
```

その2日間、予想と通知は普通に来ていたので、**結果の取り込みだけ止まっていることに誰も気づけなかった**。
→ §8 の「中身が古くなっていないか」の見張りを入れた。

### 4-3. なぜ「3分おきに1回ずつ」か

- 1回の起動で、締切 15〜2分前に入っているレースだけを見て終わる。
- 1レースは 15分の窓の中で 4〜5回見られる。1回取りこぼしても次の周が拾う。
- 1回の持ち時間（v24 は150秒）を決め、超えたら残りは次の周へ回す。
- 途中で落ちても、失うのはその3分ぶんだけ。

---

## 5. runner.py：1回の実行でやること

```
task.bat yosou
  → python runner.py yosou --v22 C:\boat\v22 --log-dir C:\boat\logs
     1. 二重起動の防止（logs\yosou.lock）
     2. リモートの取り込み（予備運転などで GitHub 側が進んでいたら拾う）
     3. 本体を動かす（yosou.py --budget 150 など。打ち切り時間つき）
     4. 変更があれば commit して push（失敗しても何も消さない）
     5. ログ（C:\boat\logs\yosou_日付.log、1行ずつ時刻つき）
```

### 5-1. ロック

- `O_CREAT | O_EXCL` でロックファイルを作る。あれば「もう動いています」で何もしない（終了コード 2）。
- 強制終了されるとロックが残るので、**一定時間より古いロックは捨てる**。
  長すぎるとその間ずっと何もしなくなる。v24 は「タスクの打ち切り時間＋5分」（最低15分）。

### 5-2. リモートの取り込み（ここが肝）

JSON を git に混ぜさせない。`rebase` / `merge -X theirs` / `push --force` は使わない。

```
fetch → 手元のデータを一時フォルダへ退避 → reset --hard origin/main
      → 退避したデータを「意味を見て」載せ直す
```

| データ | 載せ直し方 | 理由 |
|---|---|---|
| `history.json`（1行の大きな JSON） | `merge_history.py` で日付・レースごとに統合 | git から見ると常に同じ行の衝突。片方を採ると精算した的中・払戻が消える |
| `state/notified_*.json`（通知済み） | **和集合** | 片方の通知済みが消えると二重通知・二重投票になる |
| `cache/` `motor/` など | 手元を優先 | |

- **fetch できなければ何もしない**（ネットが無いときに手元を壊さない）。
- **データ以外のファイル（コード）を手で直しかけていたら取り込みを見送る**
  （`reset --hard` で消さないため）。ログにその旨を出す。

### 5-3. 保存と push

- 保存対象はデータのフォルダとファイルだけ（v24: `state` `cache` `motor` `before` `odds_snap`, `history.json`）。
- 変更が無ければ commit しない。push に失敗しても何も消さず、次の周がまた push する。

### 5-4. 終了コード

| コード | 意味 |
|---|---|
| 0 | 正常 |
| 1 | 本体が失敗（途中までを commit して終わる） |
| 2 | もう動いていたので何もしなかった |
| 3 | フォルダの指定が違う |

---

## 6. データの置き場

| 何を | どこに | git | 頻度 |
|---|---|---|---|
| 買い目・結果 | `history.json` | 入れる | 毎回 |
| 通知済み | `state/notified_日付.json` | 入れる（和集合で統合） | 毎回 |
| 出走表の先取り | `cache/` | 入れる | 朝 |
| 判定時オッズ（全組） | `snap/日付.jsonl` | **入れない** | 毎回1行追記 |
| ↑を1日分まとめたもの | `odds_snap/日付.json.gz` | 入れる | 翌朝1回 |
| 直前情報（展示・気象など） | `before/日付.json.gz` | 入れる | 翌朝1回（1〜3日前ぶん） |

- ★**3分おきに大きなファイルを commit しない。** 1日300回の commit で履歴が膨らむ。
  毎回の記録は git 管理外に追記し、翌朝まとめて1回だけ commit する。
- 翌朝のまとめは「前日〜3日前」を見る。PC が1〜2日止まっていても次の朝に取り戻せる。
  既にある日はすぐ戻る（二度取りしない）。まとめを2回やっても消えない作りにする。
- 記録系は失敗しても本体（予想・通知）を止めない（try/except で包み、理由をログに出す）。

---

## 7. オッズ取得の実務（PC から）

### 7-1. 1回の起動の流れ（yosou.py）

```
1. 各場の締切時刻を取る（1日1回取れたら cache。取れなかったら次の周で取り直す）
2. 締切まで 15〜2分 のレースを選ぶ（通知済みは飛ばす）
3. レースごとに：
     時計を取り直す（2分を切ったら見送り）
     直前情報 → 出走表（cache を優先）→ オッズ → 予想 → 買い目
     判定に使ったオッズを snap/ に1行追記
     買い目があれば通知（ntfy）して、通知済みに記録
4. 持ち時間（150秒）を超えたら残りは次の周へ
```

### 7-2. 取得まわりの決まり

- `requests.Session` を使い回す（User-Agent を付ける）。タイムアウト 15〜20秒。
- リトライは1〜2回、間に0.6〜1秒。それ以上は次の周に任せる。
- **通信失敗と「開催なし」を区別する。** 締切時刻の取得が通信失敗しただけなのに
  「開催なし」と cache に焼き付けると、その日一日その場を見なくなる（ログにも出ない）。
- 取れなかった理由（例外の種類、本文の文字数）を必ずログに出す。
- 取得元に負荷をかけない（並列で叩かない。1リクエストごとに間をあける）。

### 7-3. 先取り（prefetch）

- 出走表の数値は日中変わらないので、**朝（7/10/13時）に当日分を取って cache に入れる**。
  締切前の短い窓では取りに行かずに済み、窓の中の通信が減る。
- 取れなかった分は本番の窓でその都度取る。何度回しても cache にあるものは飛ばすので、
  回すたびに埋まっていく。

---

## 8. 見張り（止まったことに気づく仕組み）

| 見張り | どこで | 何を見るか |
|---|---|---|
| watchdog | GitHub Actions 毎時（9〜23時） | `history.json` の `last_run` が30分以上前／今日の日付が無い → 通知（優先度5）。**push が止まっているだけでも鳴る** |
| daily | GitHub Actions 23:50 | 今日一度も動いていない／**結果が2日以上入っていない**／モーター純度が古い → 通知 |
| preflight.py | 手動・朝 | モデルと特徴量の一致、データの鮮度など |
| pc_selftest.py | 手動 | 取り込み（和集合・結果が消えない）、ロック、push 失敗、文字コード、改行を、外に触らずに確かめる |

- 止まっている間は毎時鳴らす（気づくまで鳴らすのが目的）。正常なときは鳴らさない。
- ★「動いているか」だけでなく「**中身が新しいか**」も見ること。§4-2 の事故は
  「動いてはいるが、結果の取り込みだけ止まっていた」だった。

---

## 9. 予備運転と切り替えの順番

- Actions の yosou 等は `schedule:` を消して `workflow_dispatch:` だけ残し、
  **PC が止まったときにスマホから手動で動かす予備**にしている（3時間5分動く）。
- ★GitHub の定期実行は **既定のブランチ（main）の .yml だけ**を見る。作業ブランチで
  `schedule:` を消しても、main に残っていれば動き続ける。**main への取り込みが切り替えの瞬間。**
  PC のタスクが動くのを確かめてから取り込む（逆にすると、どちらも動かない時間ができる）。
- **両方が定期で動く日を作らない。** PC を戻すときは、Actions 側が終わった（か Cancel した）のを
  確かめてから PC のタスクを再開する。取り込み（§5-2）が予備運転の記録と通知済みを持ってくる。

移行のときは段階を踏んだ（v24）:

| 段階 | やること | 合格 |
|---|---|---|
| 1 | 3日並走。Actions はそのまま、PC は通知なし（`--dry`） | 同じレースに同じ組が出る |
| 2 | PC を有効にし、動いたのを見てから main に取り込む | その日のうちに通知とサイト更新 |
| 3 | 昼に yosou を1時間止める | watchdog の警告が1〜2時間以内 |
| 4 | PC を止めてスマホから予備運転 → 戻す | 二重通知が出ない |
| 5 | 昼に PC を再起動 | ログオンしなくてもタスクが再開する |

段階1で組がずれたら、まず文字コード（`PYTHONUTF8`）とデータの日付を疑う。

---

## 10. 自動投票との関係（同じ PC で動かす場合）

- 自動投票は `history.json` を**読むだけ**（書くと予想側の push と衝突する）。
  手元のファイルが「今日の分があって `last_run` が30分以内」のときだけ使い、
  そうでなければ GitHub の raw に切り替える。
- **通知された組をそのまま買う。** 投票の時点でオッズを見直して買う買わないを決め直すと、
  検証していない別のルールになる。
- 画面付きのブラウザを使うので、このタスクだけ「ユーザーがログオンしているときのみ実行」。
- 設定ファイル（口座など）は git で配らない（見本だけ配る）。配ると `reset --hard` で毎回戻される。
- ★実機で踏んだこと：**入力が速すぎて、投票票（スリップ）の反映を待たずに次へ進み、
  4組が重なって入った**。「合計ベット数 N ベット」の表示を読んで、
  数が合うまで待ってから次へ進むように直した。画面の反映は必ず待つこと。

---

## 11. 踏んだ地雷の一覧

| 症状 | 原因 | 対策 |
|---|---|---|
| push で固まり、以後ずっと「まだ動いています」 | 画面の無いタスクで認証の窓 | 画面のある窓で1回 push、`GIT_TERMINAL_PROMPT=0` など |
| motor だけ2日間動いていない | `RestartInterval` の書式、エラーで止まらない | `"PT15M"`、`-ErrorAction Stop`、最後に数える |
| JSON が毎回全行変更 | CRLF | clone の前に `core.autocrlf false` |
| 日本語が化ける | cp932 | `PYTHONUTF8=1` |
| 二重通知 | 通知済みを片方で上書き | 和集合で統合 |
| 的中・払戻が消える | git に JSON を混ぜさせた | リモートを土台に置き直し、意味を見て統合 |
| 結果が入らない | motor が失敗すると精算まで届かない | 精算は motor と切り離して必ず走らせる |
| その場を一日見ない | 通信失敗を「開催なし」と保存 | 戻り値で区別する |
| 定期実行が止まらない | 作業ブランチで schedule を消しただけ | main に入れて初めて効く |
| 自動投票で組が重なる | 画面の反映を待たずに次へ | 表示を読んで数が合うまで待つ |

---

## 12. 競輪で決め直すこと

| 項目 | 競艇 v24 | 競輪で確かめること |
|---|---|---|
| 開催の時間帯 | 7:57〜23:57 を3分おき | モーニング・デイ・ナイター・ミッドナイトがある。**最初と最後のレースの締切から yosou の起動時間を決める**（深夜を跨ぐなら日付の扱いも） |
| 判定の窓 | 締切15〜2分前 | 投票サイトの締切（本場との差）、判定→通知→投票にかかる時間から決める |
| 1回の持ち時間 | 150秒 | 3連単は最大504組。1レースの取得時間 × 同時に窓に入るレース数で決める |
| 起動間隔 | 3分 | オッズの更新間隔より細かくしても意味が無い |
| 翌朝のまとめ | 6:00 | 最終レースの後、翌日の最初の起動より前 |
| 見張りの時間帯 | 8:40〜23:10 | 開催の時間帯に合わせる |

---

## 13. v24 の該当ファイル

| ファイル | 中身 |
|---|---|
| `pc/README.md` | PC 運用の詳しい手順（この資料の元） |
| `pc/setup.bat` `pc/setup.py` | 準備と試し運転 |
| `pc/tasks.bat` `pc/register_tasks.ps1` | タスクの登録（§4） |
| `pc/task.bat` | タスクから呼ばれる入口（`PYTHONUTF8`、PATH、runner の起動） |
| `runner.py` | 取り込み・実行・保存・push・ロック・ログ（§5） |
| `merge_history.py` | history.json の統合 |
| `yosou.py` | 本体（窓・持ち時間・通知・判定時オッズの記録） |
| `prefetch.py` | 出走表の先取り |
| `official.py` | 締切時刻とオッズの取得 |
| `snap_pack.py` `collect_before.py` | 翌朝のまとめ（§6） |
| `watchdog.py` `daily.py` | 見張り（§8） |
| `preflight.py` `pc_selftest.py` `tests/` | 確認 |
| `auto_bet/` | 自動投票（§10） |
