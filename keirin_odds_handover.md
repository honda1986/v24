# 引き継ぎ資料：リアルタイムオッズの取得と予想への組み込み（競艇 v24 → 競輪アプリ）

2026-09-25 作成。競艇予想 v24（honda1986/v24）で約1年かけて作り、実弾で回している
「オッズを予想に組み込む仕組み」を、競輪アプリへ移すためにまとめたもの。

- **§1〜§4 はそのまま移せる考え方と式**。競艇の数字は例として残してある。
- **§5 の検証の作法がいちばん大事。** ここを外すと、バックテストだけ良くて本番で負ける。
- **§7 は競輪で必ず確かめ直すこと**（競艇の値をそのまま使ってはいけない所）。
- v24 の該当コードは §8 に一覧。

---

## 1. 全体像

```
 締切 15〜2分前（3分おきに起動）
      │
      ▼
 ① オッズ取得 ───────────────┐ 判定に使ったオッズを毎回そのまま保存（§2-5）
   3連単の全組（競艇120組）    │
      │                        ▼
      ▼                    snap/日付.jsonl → 翌朝まとめて保存
 ② 市場確率 q に変換（§3）
   q = (1/オッズ) を合計1に正規化
   q1 = 各車の「1着になる」市場確率
      │
      ▼
 ③ モデル（§4）
   特徴量 = 出走表などの情報 ＋ 市場の情報(log q1 など)
   出力 = 各車の1着確率 p1
      │
      ▼
 ④ 3連単の確率 p に展開（§4-2）
   p(a,b,c) = q(a,b,c) × p1(a)/q1(a)   ← 2着・3着は市場の形を借りる
      │
      ▼
 ⑤ 買い目（§4-4）
   q が決めた帯の中で  p/q > しきい値  の組だけ買う
```

**考え方の芯**: モデルに「ゼロから着順を当てさせる」のではなく、
**市場（オッズ）を土台にして、市場がずれている所だけを直させる**。
オッズは数千人の判断の集計なので、それ単体で出走表だけのモデルより強い。
勝ち筋は「市場より少しだけ正確な所を見つけて、そこだけ買う」しかない。

---

## 2. オッズの取得

### 2-1. 何を取るか：3連単の全組

- 競艇では **3連単120組を毎回すべて取っている**。理由:
  1. 全組そろえば、1着・2着・3着の市場の見立て（条件付きの形）がすべて分かる（§3・§4-2）。
  2. 3連単は売上が最も大きく、オッズが最も安定している。
  3. 他の券種（2連単・単勝など）は 3連単の q を足し合わせれば出せる。
- 競輪は最大9車なので **3連単は最大 9×8×7 = 504組**。7車立てなら 210組。
  **車立て（出走数）がレースごとに違う**ので、組の並びは固定長の配列ではなく
  「そのレースの出走車番から作る」こと（§7）。

### 2-2. 取得元の選び方（競輪で最初にやる調査）

v24 は公式サイト（boatrace.jp）の PC版ページを requests + BeautifulSoup で読んでいる。
競輪の取得元（公式・各投票サイト・オッズ配信サイトなど）を選ぶときに確かめる項目:

| 確かめること | 理由 |
|---|---|
| 3連単の**全組**が1ページ（か少ない回数）で取れるか | 504組を何十回にも分けて取ると締切に間に合わない |
| **JavaScript で描いていないか**（requests で取った HTML に数字があるか） | 競艇はスマホ版が JS 描画で使えず、PC版だけが頼りだった。JS なら Playwright が要り、重く遅い |
| 更新の間隔（何分ごとに数字が変わるか） | 3分おきに取っても5分おきにしか変わらないなら無駄 |
| 締切の何分前まで出ているか・締切後に確定オッズが残るか | 確定オッズは検証（§5）に必須 |
| 過去日のオッズが後から取れるか | 取れないなら今日から貯めるしかない（§2-5） |
| 利用規約・アクセス間隔 | 1リクエストごとに 0.5〜1秒あける。並列で叩かない |

★**後から取れない情報は、決めた日から毎日貯め始めること。** v24 では直前情報
（展示・風向など）を保存していなかったせいで、検証したいときにデータが無かった。

### 2-3. v24 で踏んだ地雷（取得のコード）

1. **数字の書き方が桁で変わる。** 競艇の公式は 999.9倍までは "890.6"、
   1000倍以上は "1059" のように**小数点なしの整数**。「小数点が必須」の正規表現にすると
   1000倍以上が全部落ちて、組が欠ける。→ 取った組数が期待どおりか毎回確かめる。
2. **通信失敗と「開催なし」を区別する。** 一度の通信失敗を「その場は開催なし」と
   キャッシュに焼き付けると、その日一日その場を見なくなる（しかもログに何も出ない）。
   戻り値を `(値, 確定したか)` の2つにする。
3. **欠けた回を黙って使わない。** オッズが全組そろっていない・0や空が混ざっている回は
   `None` を返して見送る（`market_probs` が形と正の値を確認している）。
   競輪は**欠車**で組が減るので、「そろっている」の定義を出走車から決める。
4. **リトライは1〜2回、間に1秒。** それ以上は次の周（3分後）に任せる。
   1回の起動に持ち時間（v24 は150秒）を設け、超えたら残りは次の周へ。
5. **取れた中で一番良いものを残す。** 直前情報は、2回目が1回目より欠けていたら
   1回目を残す（点数をつけて比べる）。
6. **本文の長さでおかしなページを見分ける。** 開催していない日・場のページは極端に短い
   （競艇は約13KB）。解析できなかったら「本文 N 文字」を出して原因を追えるように。
7. **User-Agent を付けた Session を使い回す。**

### 2-4. いつ取るか：判定の窓

- v24 は **締切 15分前〜2分前** のレースを、**3分おき**に見て判定する。
- 「締切」は**ネット投票の締切**（競艇は本場締切の3分前）で数える。
  判定→通知→投票に 10〜30秒かかるので、下限を詰めすぎると間に合わない。
- ★**なぜ締切に寄せるか**: オッズは締切直前まで動き、払戻は**確定オッズ**で決まる。
  v24 の実測（買った94点）では
  - 確定オッズ ÷ 判定時オッズ = **中央 0.96**（約4%下がる）。上位8組では中央 0.93。
  - 判定時に「買い」だった組のうち、**確定オッズでも条件を満たしたのは53%だけ**。
    残り47%は「たまたま高かった瞬間」を拾っていた。
  - 判定時オッズで計算した回収率 101% に対し、実際の払戻では 93%（的中18本で誤差は大きい）。
- 締切に近いほど確定オッズに近い。ただし窓を狭めると1レースを見る回数が減り、買い目も減る。

### 2-5. 判定時オッズを必ず保存する

- v24 は判定のたびに `{"t":時刻,"jcd":場,"rno":R,"left":締切まで分,"odds":[全組]}` を
  `snap/日付.jsonl` に1行ずつ追記している（git には入れない）。
- 翌朝のタスクが前日分を `odds_snap/日付.json.gz` にまとめて保存する。
  **3分おきに git へ commit すると履歴が膨らむ**ので、1日1回にまとめる。
- これが無いと「バックテスト（確定オッズで選ぶ）と本番（締切前オッズで選ぶ）の差」が
  測れない（§5-2）。**競輪では最初の日から貯めること。**

---

## 3. オッズ → 市場確率 q

### 3-1. 式

```python
def market_probs(odds):                 # odds: 3連単 全組（そのレースの組の並び）
    od = np.asarray(odds, float)
    if not np.all(od > 0):              # 欠け・0・None があれば使わない
        return None, None
    inv = 1.0 / od
    q = inv / inv.sum()                 # 各組の市場確率（合計1）
    q1 = np.array([q[FIRST == a].sum() for a in range(N)])   # 各車の1着確率
    return q, q1 / q1.sum()
```

- `FIRST[i]` は組 i の1着の車（0始まり）。N は出走数。
- 同じように、2着・3着の条件付き確率も q を足して作れる（§4-3）。

### 3-2. Z = Σ(1/オッズ) で控除を確かめる

- Z は「1 ÷ 払戻率」にほぼ等しい。競艇 3連単は **Z ≈ 1.337**（払戻率 約75%）。
- ★**Z は「収支トントンに必要な精度」そのもの。** 市場の確率どおりに買うと
  回収率は 1/Z ≈ 75%。**実際の的中率 ÷ 市場の確率 が Z（1.337）を超えて初めて黒字**。
  v24 はバックテストの結果を毎回この「実測/市場」で見ている。
- 競輪は券種ごとに払戻率が違う可能性があるので、**自分のデータで Z を計算して確かめる**
  （§7）。オッズの丸め（10円単位など）でも少しずれる。

### 3-3. 市場の癖（v24 の実測）

- 本命側（q が大きい組）ほど回収率が高く、大穴ほど低い（いわゆる本命-大穴バイアス）。
  v24 の買い目が本命寄り（q 0.12〜0.25、オッズ約4〜6倍）なのはこのため。
- 「穴は市場が見落としている」は、少なくとも競艇ではデータで否定された。

---

## 4. 予想への反映

### 4-1. 市場の情報を特徴量に入れる（各車に5つ）

```python
d["lq"]     = log(q1)                  # 1着の市場確率の対数
d["q_rank"] = q1 の順位（1 = 1番人気）
d["q_rel"]  = lq - max(lq)             # 1番人気との差
d["q_conc"] = max(q1)（全車同じ値）     # 1強か混戦か
d["q_ent"]  = -Σ q1 log q1（全車同じ値）# レースの荒れ具合
```

- モデル（LightGBM、1着か否かの2値）は **「市場の見立て」＋「出走表などの情報」** から
  各車の1着確率 p1 を出し、レース内で合計1に正規化する。
- 出走表の数値も「レース内の偏差・順位」にすると効く（v24 は `*_dev` `*_rk`）。
- ★学習に使うオッズの時点（確定か締切前か）と本番の時点をそろえること（§5-2）。

### 4-2. 1着だけモデルで直し、2着・3着は市場の形を借りる

```python
def trifecta(p1, q):
    q1 = 各車の1着市場確率（q から）
    ratio = p1 / q1                  # モデルが市場より何倍その車を1着と見ているか
    cp = q * ratio[FIRST]            # 1着の車の倍率だけ掛ける
    return cp / cp.sum()
```

- 意味: 「a が1着のときの2着・3着の並び方」は市場の形のまま使い、
  1着の確率だけモデルに差し替える。
- v24 の前の版（v23）は1着・2着・3着のモデルを3本積み重ねていた（カスケード）。
  v24 はこの形に変え、3連単の対数損失で市場を −0.0037 下回るところまで、
  1着のモデル1本でそのまま届いた。2着・3着のモデルは要らなかった。
  独立を仮定する Harville 式（1着確率だけから2着・3着を割り振る）は、
  **競輪では特に危ない**。ラインで連れて来る組が市場の形には入っているが、
  Harville では消えてしまう。**市場の形を借りる方式は競輪向き**のはず。

### 4-3. （任意）2着・3着の補正

- v24 は後から「2着の補正 g」「3着の補正 h」を足した:
  市場の条件付き P(2着=b | 1着=a) を土台に、それを直す倍率 g(a,b) を別モデルで出して掛ける。
  `p = trifecta(p1,q) × g × h` を正規化。
- 効果は 1着の補正より小さく、期間でぶれる。**まず §4-2 だけで作り、検証が回ってから足す。**

### 4-4. 買い目：市場より高く見ている組だけ買う

```python
buy = [i for i in 全組 if Q_LO <= q[i] < Q_HI and p[i] / q[i] > PQ_MIN]
```

- v24 の本番: q 0.12〜0.25（オッズ約4〜6.2倍）の帯で p/q > 1.14。
- ★**p/q > 1 では足りない。** 控除があるので、p が正しくても p/q が Z（1.337）付近まで
  高くないと黒字にならない。実際には p の誤差があるので、しきい値は
  **バックテストで決め、決めたら動かさない**（§5-3）。
- 帯（q の範囲）を絞るのは、モデルが正確な範囲と市場の癖（§3-3）のため。
- v24 の成績（参考）: 帯の回収率はバックテストの独立期間で約100〜102%。
  **オッズを入れれば勝てる、ではない。トントン付近がやっと**という水準。

---

## 5. 検証の作法（ここがいちばん大事）

### 5-1. 期間を2つに分け、片方は最後まで見ない

- v24: 学習は 2025/03/16 まで。その後を「探索期間」と「独立期間」に分け、
  **ルールやしきい値は探索期間で決め、独立期間で1回だけ確かめる。**
- 両期間で同じ向きに出たものだけを信じる。片方だけ良いものは偶然とみなす。

### 5-2. 学習・検証・本番でオッズの時点をそろえる

- 手元の過去オッズが**確定オッズ**なら、バックテストは「確定オッズで選んで確定オッズで払う」
  ことになる。本番は「締切前オッズで選んで確定オッズで払う」。**この差が実運用の目減り**（§2-4）。
- 判定時オッズを貯めて（§2-5）、たまったら判定時オッズでバックテストを回し直す。

### 5-3. しきい値・条件を後から動かさない

- 「この会場を外すと良い」「上位1%に絞ると良い」「このR番号を外すと良い」は、
  **結果を見てから切ると、ほぼ必ず良く見える**（後知恵）。
- v24 では、60通りの帯を総当たりして「両期間とも良くなった」マスが14個あったが、
  偶然でも平均12.7個出る（P=0.38）と分かり、全部捨てた。
  **総当たりしたら、必ず「偶然ならいくつ出るか」を同じ手順で数える**
  （新旧のモデルをレースごとにランダムに入れ替える、など）。

### 5-4. 比べるときは点数をそろえ、日単位でぶれを見る

- 新しいモデルは同じしきい値でも買う点数が変わる。**点数をそろえて**（p/q の上位から同数）比べる。
- 誤差は**日単位のブートストラップ**（日を丸ごと復元抽出）で出す。レース単位だと同じ日の
  レース同士の相関を無視して誤差が小さく出る。

### 5-5. 予測の精度（対数損失）が上がっても、回収率が上がるとは限らない

- v24 で風の特徴量を足したら、1着の対数損失は両期間で改善した（−0.00067 ±0.00020）が、
  帯の回収率は独立 +5.0pt / 探索 −3.0pt で**期間によって逆**。本番には入れなかった。
- 採用の基準は「両期間で、点数をそろえた回収率が悪くならない」。

### 5-6. リーク（本番では分からない情報）のチェックリスト

v24 で実際に見つかったもの:

| 見つかったリーク | 何が起きたか |
|---|---|
| 気象（Kファイル＝レース時の値）で学習 | 本番で見られる直前情報は「1つ前のレース」の値だった。レース時の値で学習すると約2割良く見えすぎた。→ 前のレースの値で学習し直した |
| 実際の進入コース | 過去データの進入は本番の値（オッズ締切後にしか分からない）。展示の進入とは別物 |
| 確定オッズで買い目を選ぶ | §5-2 |

もう1つ、**学習と本番でデータの取得元をそろえる**。v23 は本番の出走表を学習データとは
別のサイトから取っていて、同じ項目名でも中身が違っていた（v24 で修正）。

競輪で特に疑うもの: **実際の並び（周回で決まる）と予想の並びの違い**、レース後に更新される
成績（その日の結果が入った勝率）、確定オッズ、欠車の扱い。
「そのデータが、判定する時刻（締切の数分前）に本当に手元にあるか」を1項目ずつ確かめる。

---

## 6. 運用で気をつけること

- **自分の賭けでオッズが動く。** 売上の小さいレース・組では、数百円でも確定オッズが下がる。
  競輪は開催によって売上の差が大きいので注意。
- **ドローダウンは普通に起きる。** v24 の帯（1点100円）で、最大 −1.1万円、最長28連敗、
  黒字の月は約6割。回収率がちょうど100%の戦略でも、9か月で −8,600円くらいの凹みは普通。
  **凹んだ＝壊れた、ではない。** 1点500円なら5倍。
- **賭け金を増やすのは前向きの成績で100%超えが確かになってから。** 回収率の95%区間が
  100%をまたいでいるうちは、ケリー基準の答えも「増やさない」。
- 取得の失敗は理由を必ずログに出す（「取れない」だけだと原因が追えない）。
- 保存・通知・自動投票が落ちても、予想の本体は止めない（記録系は try/except で包む）。

---

## 7. 競輪で確かめ直すこと（競艇の値を流用しない）

| 項目 | 競艇 v24 | 競輪では |
|---|---|---|
| 出走数 | 6艇固定 | 最大9車、7車立てもある。欠車で減る → 組の並びをレースごとに作る |
| 3連単の組数 | 120 | 最大504（9車）／210（7車） |
| Z（=1/払戻率） | 1.337 | **自分のデータで Σ(1/オッズ) を計算して確かめる**（券種ごと） |
| 締切 | ネット締切は本場締切の3分前 | 投票サイトごとの締切を確認 |
| 判定の窓 | 締切15〜2分前、3分おき | オッズの更新間隔と取得にかかる時間から決め直す |
| 数字の表記 | 1000倍以上は小数点なし | 取得元ごとに確認（桁・カンマ・「---」・欠車の表示） |
| 帯としきい値 | q 0.12〜0.25、p/q>1.14 | **一から探索期間で決める**（競艇の値を持ってこない） |
| 市場の構造 | 1号艇が強い | ライン（並び）で組が連動する → §4-2 の「市場の形を借りる」が効くはず |
| 事前情報 | 展示タイム・展示進入・直前の気象 | 並び予想・選手コメント・バンクの状態など。**判定時刻に手元にあるものだけ**を使う |

---

## 8. v24 の該当コード（honda1986/v24）

| ファイル | 関数・定数 | 中身 |
|---|---|---|
| `official.py` | `fetch_odds` / `parse_odds3t` | 3連単オッズの取得と解析（リトライ・取れない理由の表示） |
| `official.py` | `fetch_close` / `net_close` / `mins_left` / `NET_LEAD` | 締切時刻、ネット締切、残り分数。通信失敗と開催なしの区別 |
| `features.py` | `market_probs` | オッズ → q, q1 |
| `features.py` | `build_race`（`MARKET` の5つ） | 市場の特徴量と、レース内の偏差・順位 |
| `features.py` | `trifecta` | 1着だけモデルで直して3連単に展開（§4-2） |
| `second.py` / `third.py` | `gmatrix` / `hvector` | 2着・3着の補正（§4-3） |
| `select_rule.py` | `pick` / `Q_LO` `Q_HI` `PQ_MIN_GH` | 買い目の選び方（§4-4） |
| `yosou.py` | `WIN_LO` `WIN_HI` / `snap_odds` | 判定の窓と、判定時オッズの保存（§2-4・§2-5） |
| `snap_pack.py` | | 判定時オッズを1日1ファイルにまとめる |
| `bt.py` | | バックテスト（点数をそろえた比較、日単位ブートストラップ） |
| `band_howto.md` | §15・§19・§20・§21 | オッズの目減り、風の特徴量、判定時オッズの保存、ドローダウン |
